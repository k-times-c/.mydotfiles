if !exists("current_compiler")
  compiler python
endif

setlocal foldmethod=indent
 
" Indentation defaults following Google's shell style guide.
set shiftwidth=2 tabstop=2 softtabstop=2 expandtab
set colorcolumn=80
